# 学伴管家正式测试用例

_M8 提交版测试清单；自动化测试入口为 `backend/tests/test_api.py`，最后执行日期：2026-08-07_

---

## 🧭 测试目标

验证项目能够独立启动，并覆盖“资料上传 → 文本解析 → DDL 抽取 → 人工确认 → 任务管理 → 复习计划 → 导出”的主流程，以及上传失败、模型失败、数据库异常和空数据等边界场景。

当前自动化基线：

| 检查项 | 结果 |
| --- | --- |
| Python 依赖检查 | `pip check` 通过 |
| 后端测试 | 53 passed |
| 前端 lint | 通过 |
| 前端 production build | 通过 |
| 实际 HTTP 冒烟 | `/api/health`、`/docs`、`/openapi.json` 返回 200 |

## 🧱 测试环境与证据

| 项目 | 值 |
| --- | --- |
| 操作系统与终端 | Windows + PowerShell |
| Python | 3.14.6，项目 `.venv` |
| Node.js | 24.19.0 |
| 数据库 | SQLite 内存数据库（自动化）/ 本地 `data/app.db`（冒烟） |
| 最近一次验证 | 2026-08-07 |
| 复现入口 | `scripts/verify-local.ps1` |

自动化用例负责验证接口、业务规则和失败兜底；响应式布局、键盘操作、系统日历导入和演示数据脱敏属于手工验收，不能仅以单元测试替代。

## 🧪 正式用例矩阵

| 编号 | 场景 | 操作 | 期望结果 | 类型 |
| --- | --- | --- | --- | --- |
| TC-01 | 服务健康检查 | 启动后访问 `/api/health` | 返回 `status=ok`、`database=ok`，响应头包含 `X-Request-ID` | 自动化 |
| TC-02 | 空数据首页 | 清空测试数据后打开首页 | 显示空状态，不出现未捕获异常 | 自动化 |
| TC-03 | 课程 CRUD | 新建、编辑、删除课程 | 数据保存成功，关联资源校验有效 | 自动化 + 手工 |
| TC-04 | TXT/Markdown 上传 | 上传 UTF-8 文本 | 资料入库，解析正文和处理状态正确 | 自动化 |
| TC-05 | DOCX/PDF 解析 | 分别上传 Word 和文本型 PDF | 能提取正文并保存原始文件信息 | 自动化 |
| TC-06 | 图片或扫描 PDF 失败兜底 | 使用 OCR 不可用或失败的样本 | 保留原文件、失败状态和失败原因，可重试或手动补文 | 自动化 + 手工 |
| TC-07 | 上传边界校验 | 上传空文件、超大文件、非法扩展名、超过数量的文件 | 请求被拒绝，不产生脏的资料记录 | 自动化 |
| TC-08 | 重复上传 | 上传内容相同但文件名不同的文件 | 按内容哈希拒绝重复资料 | 自动化 |
| TC-09 | 资料检索 | 用文件名、摘要、正文和标签搜索 | 返回命中字段和匹配片段，过滤条件可组合 | 自动化 + 手工 |
| TC-10 | DDL 抽取预览 | 从通知文本执行 AI 抽取 | 返回候选任务、日期、来源片段、置信度和复核标记 | 自动化 |
| TC-11 | 人工确认建任务 | 修改候选任务后勾选并确认 | 只创建已勾选任务，确认结果可追溯 | 自动化 + 手工 |
| TC-12 | 重复确认保护 | 对已确认批次再次确认或重复抽取 | 不重复创建任务，不破坏已确认快照 | 自动化 |
| TC-13 | 任务闭环 | 创建、编辑、标记完成、删除任务 | 状态正确，已完成任务不再被标记为逾期 | 自动化 + 手工 |
| TC-14 | 复习计划 | 选择课程、考试日期和每日时长并生成计划 | 计划按阶段生成，可编辑，日期不超过考试日 | 自动化 + 手工 |
| TC-15 | 导出 | 导出任务 CSV/iCal、资料 Markdown、计划 Markdown | 文件可下载，中文编码和来源名称正确；iCal 含上海时区和提前一天提醒 | 自动化 + 手工 |
| TC-16 | 外部 Provider 异常 | 模拟超时、网络错误和非法 JSON | 保存安全失败状态，不泄露异常细节，不创建正式任务 | 自动化 |
| TC-17 | 数据库异常 | 模拟数据库不可用后访问健康检查 | 返回 503 和 `DATABASE_UNAVAILABLE` 统一错误结构 | 自动化 |
| TC-18 | 开发数据重置 | 开发环境点击“重置测试数据” | 清理业务数据和上传文件；非开发环境接口不可用 | 自动化 + 手工 |
| TC-19 | 响应式与键盘操作 | 分别使用桌面和手机视口访问主要页面，并用 Tab/Enter 操作首页卡片 | 无页面级横向溢出；移动导航、工具栏和弹窗可用；焦点清晰 | 手工 |

## ▶️ 执行步骤

在项目根目录执行：

```powershell
& .\.venv\Scripts\python.exe -m pip check
& .\.venv\Scripts\python.exe -m pytest -q
Push-Location frontend
npm.cmd run lint
npm.cmd run build
Pop-Location
```

或执行一键校验脚本：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\scripts\verify-local.ps1
```

脚本通过的判定标准是：依赖检查、后端测试、前端 lint 和 production build 均成功。若只执行单项命令，应在测试记录中注明未覆盖的检查项。

## ✅ 现场验收门槛

- [ ] 全新终端能够按照 README 启动后端和前端
- [ ] `GET /api/health`、`GET /docs` 可访问
- [ ] 演示模式只生成脱敏数据，不依赖外部模型 API
- [ ] 至少上传一份资料并看到解析结果
- [ ] 至少确认一条 DDL 并在任务页完成它
- [ ] 生成一份复习计划并导出 Markdown
- [ ] 导出待办日历并导入系统日历，确认截止时间和提醒正确
- [ ] 使用手机宽度检查底部导航、任务筛选和弹窗，不出现页面级横向滚动
- [ ] 人工复核入口和来源原文在演示中可见
- [ ] 网络、AI、OCR、上传失败的备用路径已准备
- [ ] 录屏或现场演示不包含真实姓名、学号、手机号和私人聊天记录

## 🔗 测试依据

- [后端自动化测试](../backend/tests/test_api.py)
- [测试夹具](../backend/tests/conftest.py)
- [本地校验脚本](../scripts/verify-local.ps1)
- [项目验收标准](../项目目标计划与实现思路.md#十七验收标准)

# 视觉优化验收 · 2026-09-19

本轮覆盖：首页、登录与注册共用页、工作区导航与全局排版、今日重点和专注计时。

## 已完成的检查

- `npm run verify` 全部通过：ESLint、12 项现有契约检查、生产构建和包体预算检查。
- 1440 × 900：检查首页和登录页的双栏布局、总览侧栏和今日重点。首页示例勾选后进度由 1/3 更新至 2/3，考前场景切换和截止延期示例均正常显示。
- 390 × 844：检查学习总览、任务列表、专注计时、底部导航和全部功能弹窗。弹窗关闭后焦点回到入口；页面未发生横向溢出。
- 360 × 800：检查首页和登录页，标题、按钮、表单、任务预览均在屏幕宽度内。
- 最终浏览器错误日志为空；通过本地示例登录进入真实工作区，退出后返回公开首页。
- 字体包含在本地构建中，原始 OFL 许可一并复制到发布目录。字体共约 205 KiB，无新增运行时依赖。

## 验收环境

视觉预览使用 `http://127.0.0.1:5186/`，接口位于 8019 端口，示例账号和数据库位于 `.tmp/visual-review`。既有 8000 端口服务与正式用户数据未修改。

原项目已有未提交的功能修改，本轮在现有工作区上进行视觉调整。未部署到外部服务器。

中文字体是固定界面文案子集；新增文案建议按字体说明重新生成，未收录文字仍可用系统字体正常显示。首页移除了循环漂浮与旋转装饰，保留勾选、切换和进度反馈；系统减少动态效果设置继续生效。

## 背景装饰追加验收

- 新增纸张层叠背景、书页曲线、纸页路径三组素材。内置图像生成的原图和完整提示词保存在 `frontend/src/assets/decor`，生产网页使用 25,112 字节 WebP 与两份小型 SVG；原始 PNG 没有进入构建资源目录。
- 1280px 桌面：首页首屏、通知示例、底部横幅和登录介绍区均已看图验收；1033px 工作区检查了页首细线与今日重点背景。
- 390 × 844 手机：首页、登录页、学习总览无横向溢出，装饰降低透明度，表单与正文仍清晰可读。
- 首页勾选任务后进度显示 2/3，考前场景和截止延期切换正常。检查新增装饰层均为 `pointer-events: none`。
- 追加修改后的 `npm run verify` 全部通过；入口 CSS 为 32.53 KiB gzip，低于原有 34 KiB 限制；未增加运行时依赖。

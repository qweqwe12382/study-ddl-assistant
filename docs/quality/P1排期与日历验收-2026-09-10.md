# P1 排期与日历验收记录

日期：2026-09-10。本文件区分已执行检查与尚未完成的验收，不作为比赛全部交付完成的证明。

## 范围

面向一般大学生，不以开发者或单个用户的手机型号定义适用范围。按用户最新要求，Android 仅收尾当前系统日历入口及必要验证，之后暂停移动端优化；后续重点为 Web 与比赛交付。

## Web 日历导出

任务完整视图的导出按钮先展示简短步骤：下载、核对与选择日历、检查提醒并保存。明确导出全部有截止时间的未完成任务，不受列表筛选影响；提前一天提醒能否保留取决于客户端。展开帮助说明导入入口差异、一次性导出不会自动同步、重复导入和旧日程需要自行检查。

实际浏览器检查：隔离账号中打开弹窗，确认下载按钮可见，展开导入帮助，再关闭。检查中发现异步组件缺少局部 ElDialog 导入，已修复并重新核对真实 dialog 及页脚按钮。随后前端完整 `npm --prefix frontend run verify` 退出码 0，含 lint、全部契约、构建及包体预算。此记录不代表已在任何手机日历中保存或触发提醒。

iCal 身份与版本回归见 [P0 记录的后续开发部分](P0通知闭环验收-2026-09-10.md)：两个新增身份用例与既有 API 用例共 65 passed。旧 UID 到新 UID 无自动迁移，不能据此承诺各客户端去重或同步更新。

## 排期变更与检查

任务按上海本地截止日期优先，同日按优先级分配；使用剩余分钟，其次预计分钟，缺失时明确提示按 30 分钟假设。跨日安排递减同一份工作量，资料仅使用剩余容量，不再把临近截止任务轮转到截止日之后。未排入内容显示名称、分钟数以及容量或每日主题数限制。空余时间单独标为自主复盘与缓冲，不重复挂上已排完任务的来源。

本计划的每日分钟数是独立上限，用户应填扣除上课后的可用时间；多计划尚不共享预算。截止日当天只有自然日级安排，不能保证具体截止时刻前有空闲；完成反馈校准系数只用于总体工作量风险参考，实际排期使用当前剩余或预计分钟。既有 agent 重排仍是用户确认后的内容差异，不是新的时段调度器。

2026-09-10 11:59 执行 `scripts/verify-local.ps1`：pip check 通过、后端 **231 passed**、前端完整 verify 通过，退出码 0。证据目录为 `.pytest-tmp/run-20260910-115902-a6dfda21e87b4745af2f8ea914c6ee2e/pytest`。此前直接 pytest 的一次运行有 229 passed、1 failed，失败发生在合成配置测试中 dotenv 替换临时 `.env` 时，报 WinError 5；保留该失败记录，未修改测试断言规避它。

追加时区修正后，根执行 `test_study_plan_scheduling.py`、既有 `test_api.py` 及 `test_m84_agent_feedback.py`，共 **75 passed**，证据目录 `.pytest-tmp/p1-final-20260910-1202`。新增用例固定 UTC 9 月 10 日 16:30（上海 9 月 11 日 00:30），验证计划从上海 9 月 11 日起生成。该次为受影响范围回归，没有再次执行全量后端。

## Android 当前修改已收尾

有有效截止时间的任务可打开系统日历编辑页，预填标题和截止点，结束时间为截止后 1 分钟，仅作标记，不代表学习时长。无效或缺失日期不生成入口。采用 Android 官方建议的 [Calendar intents](https://developer.android.com/identity/providers/calendar-provider)；按[包可见性指导](https://developer.android.com/training/package-visibility/use-cases)直接尝试启动并处理无可用应用或权限异常，避免查询不可见应用造成假阴性。

子代理实际执行 `gradlew.bat testDebugUnitTest lintDebug assembleDebug --console=plain` 成功；根核对 XML 报告共 **11 tests、0 failures/errors**，lint 文本为 `No issues found.`，Debug APK 已生成。未操作 native UI，未实测保存或提醒。当前修改结束后暂停移动端开发。

## 离线备份恢复

新增 `USER_DATABASE_DIR`，默认保留原位置，非 legacy 用户数据库可随隔离目录一起配置；真实工厂缓存按绝对根目录和用户身份区分。新增 `scripts/backup_data.py` 与[操作说明](../delivery/离线备份与隔离恢复.md)，以停服后的目录快照备份完整实例，清空副本登录会话，恢复仅写新目录。

2026-09-10 12:10 统一脚本再次完整通过：pip check、后端 **244 passed**、前端完整 verify，退出码 0。证据根为 `.pytest-tmp/run-20260910-121030-db825506bb5440a28175f96d9f21c36f/pytest`。新增真实工厂路径 10 项检查，以及独立子进程双账号备份/恢复与破坏性输入拒绝检查。双账号实际重新登录、下载资料、读取关联任务及计划；旧 cookie 失效；恢复后新增数据不修改原目录或备份。与此前覆盖数据库工厂的接口冒烟相比，这次使用真实 lifespan、认证和工厂，没有依赖覆盖。

备份核查文件大小/哈希、外键/SQLite 完整性、逐表行数/结构摘要、来源身份格式和 JSON；缺少元数据与孤立文件单独列出，不自动重新绑定历史来源。同版本恢复经过验证，跨版本数据库升级不在支持范围。未对真实个人数据执行备份，也未做浏览器恢复演练。

补充 `.part` 中断文件拒绝规则后，备份集成测试再次 **2 passed**（`.pytest-tmp/backup-final-20260910`）。随后针对该目录内的合成源数据实际执行 CLI `backup → restore → verify`，三个命令均退出码 0；输出位于对应用例的 `cli-backup`、`cli-restored` 及 `cli-*-result.json`。没有访问或备份真实个人数据库。

最后一轮 Sol 审查指出源用户库目录中的额外文件或嵌套数据库会被静默省略。根已在输出创建前拒绝意外条目，仅接纳根层合法用户数据库及其 SQLite sidecar（后者由 SQLite backup 吸收，不作为数据库单独复制）。加入 `note.bin` 和嵌套数据库拒绝回归后，备份与真实工厂相关检查 **12 passed**，证据为 `.pytest-tmp/backup-source-shape-20260910`。这是全量 244 项基线之后的针对性检查。

## 待完成

- 排期已在独立恢复实例完成下述浏览器联调；多计划共享预算与实际课表占用仍未实现。
- 系统日历实际导入、重复导入及提醒触发尚无客户端实测证据。
- 备份恢复已具备自动化与下述合成双账号浏览器验收；个人真实数据操作和跨版本恢复不在本次验证范围。

## 浏览器恢复与排期补验

2026-09-10 12:19 起，将此前 CLI 合成备份恢复到新的 `.tmp/browser-recovery-20260910`。分别设置四个绝对数据路径，在后端 8002 / 前端 5182 启动独立实例，保留原开发实例。浏览器首账号重新登录，看到“备份任务0”，点击任务来源成功定位 `notice-0.txt`；退出后登录第二账号，仅看到“备份任务1”和“备份课程1复习计划”，来源为 `notice-1.txt`。此处为实际浏览器操作，不以接口测试代替。

在第二账号的恢复副本内把合成任务改成 2 小时、9 月 11 日 18:00 截止，再生成两天、每日 30 分钟的新计划。浏览器显示两项各 30 分钟，任务有 60 分钟未排入、资料有 60 分钟未排入，并显示仅按自然日安排的限制；原计划仍保留。

自动化 `fill` 后曾观察到数值输入框显示旧值，代码与真实键盘复验将其定位为输入事件差异，未证明是业务回填问题，相关试探修改已撤回。最终用 AX 点击、Ctrl+A、逐键输入、Tab 离开输入框后生成：表单、元数据与每天安排均为 20 分钟，总计 40 分钟，任务未排入 80 分钟，结果一致。该现象不列为已修复的应用 bug。

本轮实际保留的 Web 改进：专注计划信息中的“每天”改为“每日上限”；任务日期输入显示格式统一为 `YYYY-MM-DD HH:mm`。浏览器输入不含秒数的 `2026-09-11 18:15`，确认并保存后任务列表正确显示 9/11 18:15。接口时间格式保持原样。移动端未继续开发。

上述两处 Web 修改后，前端完整 `verify` 再次退出码 0（lint、全部契约、构建与包体预算）；浏览器验收会话的 error/warn 日志为空。后端本轮无代码修改，沿用前述 244 项全量及备份专项结果，不将前端重跑计成后端重跑。

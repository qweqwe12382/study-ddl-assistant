# Web 前后端监督开发与验收

本轮范围：用户授权 GPT-5.6 子代理开发，由主代理确定问题、审查实现并集成验证。Android 优化暂停，既有移动端修改保留；本记录不代表比赛全部交付完成。

## 分工与实际问题

- GPT-5.6 Sol：统一任务页简洁/详细视图的关键词、课程、状态筛选，维护来源身份定位和完成反馈。
- GPT-5.6 Sol：修复复习排期的已过时刻 DDL、零剩余工作量及无来源填充；增加生成时未排入工作的结构化反馈与页面展示。
- GPT-5.6 Terra：独立编写并冻结 16 条合成通知挑战集，保存首次异常与逐例失败，再处理异常隔离。样本首次运行后已成为已知回归集。
- 主代理：审查与联调；修复专注计时回调节流、暂停后切换任务导致错记风险、成功记录无可见回执、加载失败无重试，以及本地开发主机名不一致导致登录循环。

## 本轮浏览器实测

使用现有本地服务及内置演示账号。专注页的计时状态检查未向任务保存用时；任务页筛选不修改学习数据。浏览器验收期间后端是此前已启动、未热重载的进程，不能据此认定本轮新后端逻辑已经通过浏览器联调。

1. 原启动地址 `http://127.0.0.1:5173/` 使用了指向 `http://localhost:8000/api` 的开发配置，演示登录回到登录页；修复本地开发主机名匹配后，重新从该地址一键进入“演示同学”的学习总览。生产模式、HTTPS 与远程 API 配置不参与该转换。
2. 专注开始时任务、预设时长和自定义输入禁用；暂停后仍禁用，显示累计计时并提供继续/放弃。计时计算另有覆盖延迟回调、暂停恢复小数秒、目标上限的契约检查。
3. 演示工作区两项任务中，搜索“二叉树”得到一项；切换完整视图后保留关键词与单项结果；一键清除恢复两项。
4. 搜索不存在的任务返回零项并展示清除筛选入口；点击后恢复两项。“进行中”状态筛选得到对应的一项。
5. 390×844 Web 视口中，关键词、课程、状态纵向排列，页面宽度实测 375px，未超出 390px 视口。这是 Web 布局验收，不是继续开发原生移动端。

尚未实测浏览器连续 15 分钟后保存、系统休眠计时、真实屏幕阅读器、系统日历提醒或真实学生试用。后台定时回调的纯逻辑测试不能证明使用者实际保持专注。

## 独立输入证据

冻结挑战集及首次失败记录见 [首次评测](evaluation/supervision-holdout-first-attempt-20260910.md)。原始 SHA-256 为 `BDCECC3B930DF912BCE03B79B50A2069CD06E6D7389B2D0AF6C6940CE9BA0245`。

首次运行在已撤销的历史时间“24:00”处抛出异常，未产生完整聚合成绩；不能把异常样本排除后宣称完整通过。严格名称比较也会将标题缩写计为错误，因此需要同时查看逐例结果。日期未给时刻的通知还需区分整日截止约定与应保留歧义的表达。合成样本不是实际学生/OCR 准确率。

## 集成验收

首次完整后端运行：276 passed、3 failed。三处失败均是旧测试在空课程下期待自动生成的占位项；现已在夹具中创建真实课程资料，并保留原身份、并发和队列断言。这三个文件复测 32 passed。

首次前端最终检查：契约和构建通过，复习页 JS 为 16.06 KiB，超过既有 16 KiB 预算。将仅简洁视图使用的复习周面板改为按需加载，另设 JS 4 KiB、CSS 2.25 KiB 独立上限，既有页面预算未提高。空计划提示也明确说明补充课程资料或调整任务后需要重新生成。

独立挑战集最终已知集结果：严格任务名 TP 6、FP 8、FN 10；明确截止匹配 4/10、标空时间匹配 1/6；案例异常从 1 降为 0。无效截止与答疑时间混合时保留歧义，“中午前/课前”等没有具体时刻的限制不再填成 23:59；普通日期仍维持现有整日截止约定。结果不能包装为陌生通知准确率提高到满分。

边界：空计划后来新增任务时，现有计划差异服务没有可编辑的计划项，不会生成差异候选，需重新生成计划。排期按每份计划的日额度计算，未与其他计划共享容量，也未读取课表/考试空闲时段。生成时未排入列表是快照，后续手工修改不会把它变成实时排程结论。

按用户最后指示，本轮修复、验收及文档收尾后暂停目标开发，不开启下一轮；后续缺口仅保留为待办。

## 最终统一验收结果

2026-09-10 13:27 执行 `scripts/verify-local.ps1`，退出码 **0**：Python 依赖检查通过，后端 **279 passed，1 warning**（24.97 秒），前端完整 verify 通过。后端仅保留既有 Starlette/httpx 弃用提示；前端构建保留既有 VueUse PURE 注释提示。

最终 gzip：Tasks 主包 **14.16/18 KiB**，独立任务议程 **5.19/6 KiB**；StudyPlans 主包 **13.60/16 KiB**，独立复习周面板 **3.48/4 KiB**；Focus 主包 **3.57/10 KiB**。所有 CSS 与其他既有预算同时通过，未提高原页面预算。

实际日志：`.tmp/supervision-final-verify-r2-20260910.log`；隔离测试目录：`.pytest-tmp/run-20260910-132733-a8e9c10cb40a4f91a9ffd5ec4beb6b02/pytest`。首次失败日志继续保留在 `.tmp/supervision-final-verify-20260910.log` 和 `.tmp/supervision-frontend-final-20260910.log`。最终差异格式检查通过。

本轮所有子代理已交付停止。未提交 Git、未发布、未继续移动端开发。代码层已通过上述检查；当前常驻后端进程没有热重载，需重启后才会使用新排期与解析逻辑，不能把旧进程页面当作新后端浏览器验收。

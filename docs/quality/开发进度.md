# 开发进度

_M8 智能体能力增强开发记录 · 最后核对：2026-09-02_

---

当前阶段：M8“智能体能力增强”进行中。M0—M7 已完成，题目要求的基础链路已具备；当前不推进案例、视频和彩排，而是把网站升级为具备感知、建议、确认、执行和反馈闭环的学习智能体。

## 📋 当前状态

| 项目 | 当前值 |
| --- | --- |
| 里程碑 | M8.31“Android 移动伴侣 MVP”已完成首版；M8 智能体能力增强进行中 |
| 后端验证 | 当前完整回归 197 项通过；新增课表单双周筛选、真实时间冲突、考试北京时间、标准日历与南理工教务解析/预览/幂等同步/本地修改保护、凭据防回显、身份版本编辑与课程级联回归；邮箱认证、账号隔离和 M8.30 的 24 项编辑并发回归继续通过；仅有既存 Starlette/httpx 弃用警告 |
| 前端验证 | 2026-09-02 `npm run verify` 通过，覆盖 lint、安全来源导航、编辑前置条件、首轮引导挂载/前置条件、计划差异完整展示、production build 和 gzip 体积预算；build 仅保留既有 `@vueuse` PURE annotation 警告 |
| Android 验证 | 2026-09-02 `testDebugUnitTest`、`lintDebug`、`assembleDebug` 通过；API 36.1 模拟器使用隔离账号和数据完成登录、今日、课表、考试、DDL、设置页及带 CSRF/`If-Match` 的任务完成写回；Debug APK 已生成 |
| 本地验证 | 2026-09-02 使用隔离账号库与学习工作区验证注册、登录、会话恢复、课程写入、退出和受保护路由；既有自动抽取、今日行动、反馈、差异重排与回执闭环继续由完整回归覆盖 |
| 视觉基线 | 2026-09-02 新增公开主页、注册/登录、课表考试页和教务日历接入面板；桌面与 375×840 真页面无横向溢出。学期日程桌面显示七日课表，手机按天查看，六项底栏可触达；接入三步面板、考试表单完整可见，控制台无警告。不据此声称真实屏幕阅读器、实体键盘或系统级大字号已验收 |
| 当前产品状态 | M8.1—M8.30 已实现，并交付 M8.31 原生 Android 移动伴侣首版；另完成公开主页、本地邮箱账号、个人课表/考试、标准 iCalendar 与南京理工大学本机教务接入。移动端聚焦今日行动、课表考试和 DDL 完成，南理工凭据仍只由网页版一次性本机流程采集并立即清除 |
| 当前待办 | Android 后续补本地通知、弱网恢复、可选安全离线缓存、正式签名与真机辅助技术验收；账号仍缺邮箱验证、密码找回、登录限流、备份恢复和生产运维审计；实际用户库升级与公网部署均须另行验收 |

当前开发方向入口：[`README.md`](../../README.md)、[比赛要求对照与产品开发方向](../product/比赛要求对照与提交状态.md)、[架构与技术路线](../product/架构与技术路线.md)和[项目目标计划与实现思路](../product/项目目标计划与实现思路.md)。正式用例与演示清单保留到 M9 使用。

## 🧱 M1——后端骨架与数据库

状态：已完成

已完成：

- FastAPI 入口和 `GET /api/health`；
- 环境变量配置和 `.env.example`；
- SQLite 连接、目录创建和自动建表；
- Course、Material、Task、StudyPlan 数据模型；
- 课程、资料、任务的基础增删改查接口；
- 任务完成接口；
- Pydantic 请求/响应模型和统一校验错误结构；
- 8 个基础测试用例。

已验证：

- 后端依赖可从 `.venv` 导入；
- `pytest -q` 应通过 8 个测试；
- 启动服务后 `/api/health` 和 `/docs` 可访问。

遗留问题：

- 当前还没有文件上传、PDF/Word/TXT 解析和 OCR；
- M1 阶段尚未包含前端页面；M2 已补齐基础页面。
- 任务日期的逾期自动判断、资料文件物理存储将在后续阶段实现。

M1 稳定性优化：

- 使用 SQLite 内存数据库，规避 Windows 系统临时目录权限问题；
- 使用 UTC 时间函数替代已弃用的 `datetime.utcnow`；
- 创建和修改资料/任务前校验关联课程与资料；
- 数据库约束异常自动回滚并返回统一错误；
- 增加未处理异常日志，并支持通过环境变量配置 CORS 来源。

M1 阶段完成，进入 M2。

## 🖥️ M2——前端框架与手动管理

状态：已完成

已完成：

- Vue 3、Vite、Element Plus 基础入口和统一布局；
- Vue Router 页面路由；
- Axios 请求实例、错误提示和后端 API 封装；
- 学习总览首页，展示任务、临近 DDL、逾期任务和资料统计；
- 资料库页面，支持资料记录新增、编辑、删除和关键词筛选；
- DDL 任务页面，支持任务新增、编辑、删除、状态筛选和标记完成；
- 设置页面，支持课程新增、编辑和删除；
- 前端构建脚本使用 Vite runner 配置加载器，规避当前 Windows 环境的默认配置加载权限问题。

已验证：

- `npm.cmd run build`：构建通过；
- `npm.cmd run lint`：通过；
- 后端 `pytest -q`：7 个测试通过；
- 前端默认 API 地址为 `http://127.0.0.1:8000/api`。

下一阶段：M3——文件上传与内容解析。

## 🛠️ M2 优化——稳定性与体验

状态：已完成

- 后端在查询任务时自动将未完成且已过截止时间的任务标记为逾期；
- 前端顶部增加 API 连接状态和重试按钮；
- 增加逾期任务自动同步测试；
- 继续保持前后端错误提示和空数据状态。

## 📥 M3——文件上传与内容解析

状态：已完成

已完成：

- 新增单文件/多文件上传接口，限制扩展名、MIME 类型、空文件和文件大小；
- 原始文件使用 SHA-256 前缀安全命名，保存到 `data/uploads/`，并检测重复内容；
- 支持 TXT、Markdown、PDF、DOCX 文本提取；
- 接入 RapidOCR ONNX，支持常见图片和扫描版 PDF 识别，并在失败时保留原文件和错误原因；
- 资料记录增加文件大小、内容哈希、处理状态、解析文本和失败原因；
- 新增失败资料重试、原始文件下载和资料详情展示；
- 前端资料库增加拖拽上传、上传解析提示、失败状态、重试和详情弹窗。

已验证：

- 后端 `pytest -q`：22 个测试通过，其中 14 个为 M3 测试；
- 前端 `npm.cmd run lint`：通过；
- 前端 `npm.cmd run build`：通过。

当前遗留问题：

- OCR 识别结果仍需用户核对，DDL 自动抽取属于下一阶段 M4。

下一阶段：M4——AI 抽取与人工确认。

## 🔒 M3 优化——安全性与一致性

状态：已完成

- 手动资料接口不再允许客户端写入原始文件路径、哈希和处理状态等服务端字段；
- 上传增加单次文件数量限制、受限读取、原子写入和异常清理；
- 内容哈希增加唯一索引，降低并发重复上传风险；
- 删除资料时同步删除原始文件，重试时根据实际存储格式解析；
- 增加 DOCX 解压大小、图片分辨率和文本控制字符校验；
- 扫描版 PDF 增加页数受限的 OCR 兜底，并使用锁保护 OCR 引擎并发调用；
- 新增上传策略接口，前端动态读取文件大小、数量和格式限制；
- 前端支持手动补充解析文本和下载原文件。

## 🤖 M4——AI 抽取与人工确认

状态：已完成，稳定性优化完成

已完成：

- 在 `Material` 中增加抽取状态、Provider、结果快照、错误原因和完成时间，并保留 SQLite 轻量迁移；
- 增加可替换的 `LLMProvider`，默认使用离线 `local-rules`，配置模型环境变量后支持 OpenAI-compatible Provider；
- 增加课程识别、任务类型、DDL、`source_quote`、置信度和 `need_review` 结构化结果；
- 对模型 JSON、日期、时间、相对日期、多日期、日期冲突和来源原文执行后端校验；
- 低置信度、缺失日期和异常结果只进入待确认预览，不直接创建正式任务；
- 完成“AI抽取—人工编辑—确认创建任务”的前端流程；
- 新增 9 个 M4 测试，覆盖正常抽取、缺失日期、日期冲突、非法 JSON、人工确认、重复抽取保护、元数据编辑、人工修改快照和英文日期。
- 修复已确认结果被误标记为失败的问题，保留外部 Provider 失败前的结果快照；
- 资料元数据编辑不再无条件重置抽取状态，确认结果会同步保存课程、资料类型、标签和人工修改后的任务；
- 前端打开已有结果时优先读取快照，已确认资料不再重复调用抽取；
- 为每次抽取增加批次号和候选任务 ID，并增加数据库唯一索引，降低并发确认造成重复任务的风险；
- 外部 Provider 增加可配置超时和重试次数；
- 日期解析增加英文月份、星期/周末、相对日期和上午/下午时间，并对过去日期和默认日终时间增加提示；

接口：

- `POST /api/materials/{id}/extract`；
- `GET /api/materials/{id}/extraction`；
- `POST /api/materials/{id}/extraction/confirm`。

验证：

- 后端 `pytest -q`：33 个测试通过；
- 前端 `npm.cmd run lint`：通过；
- 前端 `npm.cmd run build`：通过。

当前遗留问题：

- 本地规则 Provider 适合演示和常见通知，复杂自然语言仍需人工核对；
- 外部 LLM Provider 需要用户自行配置 `LLM_API_KEY` 与 `LLM_MODEL`。

下一阶段：M5——检索、首页和任务闭环。

## 🔎 M5——检索、首页和任务闭环

状态：已完成

已完成：

- 资料列表支持关键词、课程、资料类型、处理状态和标签筛选；
- 搜索返回命中字段和正文/摘要匹配片段；
- 新增 `/api/dashboard`，汇总未完成、未来 7 天、逾期、已完成任务、资料和课程数量；
- 首页展示未来 7 天任务、逾期提醒、最近资料和下一步建议，并支持跳转资料库/任务页；
- 任务展示优先级和来源资料，删除资料后固化来源文件名，避免任务出现不可解释的空来源；
- 资料库和任务页复用服务端数据，刷新页面后筛选、任务状态和来源数据不丢失；
- 新增 3 个 M5 后端测试，覆盖检索命中、首页统计和删除资料后的来源策略。

已验证：

- 后端 `pytest -q`：36 个测试通过；
- 前端 `npm.cmd run lint`：通过；
- 前端 `npm.cmd run build`：通过。

## 📅 M6——复习计划与导出

状态：已完成

已完成：

- 新增复习计划生成接口，读取课程资料标签、摘要/正文片段和未完成任务，生成按天分阶段的计划明细；
- 支持考试日期、每日学习时间和计划名称输入；考试日期不能早于今天，计划明细日期不能超过考试日期；
- 对资料/任务为空、考试临近和可用时间不足提供风险提示；
- 当复习主题多于可用天数时合并多个主题，并提示覆盖压力；
- 支持编辑计划日期、主题、内容、时长和完成状态，人工修改保存在计划快照中，重新生成不会覆盖已有计划；
- 返回完成天数、总时长和已完成时长，前端实时展示完成进度；
- 支持保存计划名称、考试日期和每日学习时长，并可归档旧计划；
- 新增 DDL CSV、资料目录 Markdown 和复习计划 Markdown 导出；
- CSV 增加 UTF-8 BOM，复习计划 Markdown 显示来源资料和任务名称；
- 前端新增复习计划页面、路由、导航入口和导出按钮；
- 新增 4 个 M6 后端测试，覆盖计划生成/人工修改保留、主题合并、日期校验、归档和三类导出。

接口：

- `GET /api/study-plans`；
- `POST /api/study-plans/generate`；
- `GET/PATCH /api/study-plans/{id}`；
- `POST /api/study-plans/{id}/archive`；
- `GET /api/exports/tasks.csv`；
- `GET /api/exports/materials.md`；
- `GET /api/exports/study-plans/{id}.md`。

已验证：

- 后端 `pytest -q`：40 个测试通过；
- 前端 `npm.cmd run lint`：通过；
- 前端 `npm.cmd run build`：通过。

当前遗留问题：

- 复习计划使用本地规则生成，复杂自然语言知识点仍需要用户人工调整。

## 🛡️ M7——稳定性、部署与安全

状态：已完成

已完成：

- `/api/health` 增加数据库 `SELECT 1` 检查，数据库不可用时返回统一的 `DATABASE_UNAVAILABLE` 错误；
- 增加请求日志，记录请求 ID、方法、路径、状态码和耗时，不记录请求体、文件内容或 API 密钥；
- 响应增加 `X-Request-ID`，支持前端或日志根据同一 ID 定位请求；
- 增加 `LOG_LEVEL` 配置项，默认使用 `INFO`；
- 增加上传路径清理、文件大小限制、空课程、网络失败、模型超时、OCR 失败和数据库异常等边界测试；
- 增加默认关闭的 `DEMO_MODE`，启动时幂等写入脱敏课程、资料和任务演示数据；
- 设置页增加“重置测试数据”按钮，开发/测试环境通过 `POST /api/dev/reset` 清空课程、资料、任务、复习计划和上传文件，非开发环境关闭；
- 增加 `scripts/start-dev.ps1` 一键启动脚本和 `scripts/verify-local.ps1` 本地验证脚本；
- 补充 README、部署方式、隐私边界、已知限制和全新环境验证记录。

已验证：

- 现有环境 `pip check`：通过；
- 后端 `pytest -q`：50 个测试通过；
- 前端 `npm.cmd run lint`：通过；
- 前端 `npm.cmd run build`：通过；
- 临时全新 Python 环境安装 `backend/requirements.txt` 后，48 个测试通过；
- 临时全新前端目录执行 `npm ci`、lint 和 build 通过，npm 审计报告 0 个漏洞；
- 两个 PowerShell 脚本已通过语法检查，验证脚本完整执行通过；一键启动冒烟检查中后端和前端均返回 HTTP 200。

本阶段选择本地部署作为交付方式，暂不包含云端部署。复杂自然语言知识点仍需人工复核。

M7 阶段完成，进入 M8——智能体能力增强。

## 🤖 M8——智能体能力增强

状态：进行中。

M8.1—M8.6 完成后的当前事实：

- 首页仍保留 `/api/dashboard.next_action` 作为无智能体建议时的降级文案，但 Agent Center 已读取持久化建议、来源、原因、风险与执行回执；
- `Task` 已增加预计用时和剩余用时，学习偏好已增加每周容量、单日上限、缓冲、偏好时段和课程权重；
- Agent Center 已通过单一 briefing 汇总收件箱、今日三件事、风险、容量行动、待确认建议和最近执行回执；
- 今日行动由服务端稳定排序，使用截止层级、容量、优先级、课程权重、剩余/预计用时和同日冲突，并返回计算依据；
- 缺失估时可经 `set_task_estimate` 受控补充，临期未开始任务可经 `start_task` 进入进行中，服务端执行前重验快照并返回幂等回执；
- 当前复习计划会在新任务、任务完成、逾期和实际用时变化后评估并生成受保护的计划差异候选；用户确认后才重排，已完成或人工修改的计划项不覆盖；
- 资料上传和成功重试后已自动运行本地规则，文件名与正文联合形成候选；正式任务仍需人工确认；
- 外部 Provider 保持按次授权，自动路径固定使用 `local-rules`；
- 容量风险已同时计算七日总量、北京时间单日峰值、缺失估时和同日多 DDL；偏好时段用于行动时段建议，课程权重进入服务端排序且可在设置页编辑。

M8 分步开发：

| 子阶段 | 状态 | 内容 |
| --- | --- | --- |
| M8.1 建议契约与最小闭环 | 已完成 | 四类智能体记录、`adjust_priority` 建议、查询/接受/受限修改/忽略/过期、幂等执行与回执 |
| M8.2 收件箱与容量 | 已完成 | 自动本地抽取、文件名联合识别、学习偏好、任务工作量、七日与单日容量风险 |
| M8.3 Agent Center | 已完成 | 单一 briefing、今日三件事、容量行动、受控补估时/开始任务、课程权重、持久回执与真实页面点击闭环 |
| M8.4 反馈重规划 | 已完成 | 可选完成反馈、三样本课程校准、四类事件、计划差异重验/保护/幂等执行与真实回执 |
| M8.5 周度复盘与站内提醒 | 已完成 | 汇总近 7 天完成、逾期、计划状态、估时偏差、资料复核与回执，并提供受控行动和可持久忽略的分级提醒 |
| M8.6 复盘历史与提醒偏好 | 已完成 | 最多保存 12 个真实北京时间窗口；结束后冻结；提醒偏好由服务端校验，高风险事实始终呈现，日/周摘要仅在站内生效 |
| M8.7 证据导航与可访问性 | 已完成 | 服务端只解析白名单来源并返回固定站内目标；任务、资料、计划/计划项和固定集合能深链定位；删除来源明确不可用，历史快照不改写 |
| M8.8 学习节奏趋势与受控调整 | 已完成 | 只读汇总最近 28 个北京时间自然日的真实完成反馈；缺失不补值；四项信号与复核候选均有样本、限制和来源 |
| M8.9 学习节奏历史与谨慎比较 | 已完成 | 节奏摘要随当前周快照保存、关闭后冻结；只比较同版本同口径且样本足够的两个关闭窗口，否则显式不可比 |
| M8.10 智能体活动时间线 | 已完成 | 将事件、建议、回执和运行记录归一化为最多 50 条只读活动；不返回内部载荷或路径，来源仅以白名单引用解析 |
| M8.11 资料确认审计闭环 | 已完成 | 用户确认资料抽取并创建正式任务时，同一事务记录聚合事件；活动记录只显示固定文案和资料来源，重复/失败不会遗留事件 |
| M8.12 结构化字段来源解释 | 已完成 | 课程、资料类型和每个标签均由服务端基于文件名与正文重建来源；可诚实降级为规则推断或无法确认 |
| M8.13 今日决策台与视觉层级 | 已完成 | briefing 最多 5 条只读决策；固定排序、去重、最小来源与安全导航；首页以“来源 → 决策”突出人工下一步 |
| M8.14 学习台账前端与资料链路 | 已完成 | 统一深墨蓝学习台账外壳和“资料 → 决策 → 行动”主线；资料页用真实状态呈现“入库—识别—人工确认”，并完成桌面/375px 响应式实测 |
| M8.15 核心页面任务聚焦 | 已完成 | 首页增加今日索引与决策台锚点；任务页用真实数据汇总行动状态；复习计划按定义输入—核对依据—编辑保存呈现；设置页改为可定位的规则簿 |
| M8.16 全局简洁/详细视图 | 已完成 | 默认简洁视图优先展示行动、风险和确认入口，详细视图恢复证据、历史、完整筛选、导出与高级配置；选择跨页面保持，移动端关键操作不小于 44px |
| M8.17 可访问操作与渐进设置 | 已完成 | 路由切换聚焦 `main`、skip link 显式聚焦且同页 hash 不受影响；设置四块摘要按需切换详细区块并聚焦，支持 reduced motion；任务/资料简洁卡片分主次操作，移动底栏安全区、移动端 16px 输入和警告/灰文对比度已补齐 |
| M8.18 第一轮学习闭环与真实空状态 | 已完成 | 首页用四项真实计数显示 0/4—4/4 首轮路径；固定 action 直达课程、上传和任务；已完成任务可用新幂等键更新反馈；页面失败时不显示假 0、旧行或死 CTA |
| M8.19 今日行动舞台与渐进详细台账 | 已完成 | 默认首页只突出一项由 briefing 事实选出的主行动；高风险事项继续可见，完整 Agent Center、风险、证据和历史按需展开；多来源列表可展开且仍经 resolver 导航 |
| M8.20 渐进数据加载与前端性能预算 | 已完成 | 简洁首屏只请求 Dashboard 与 briefing/refresh；详细资源首次进入详细视图再加载且重复切换不重取；Element Plus 使用样式白名单，并以 gzip 预算阻止包体回退 |
| M8.21 复盘卷宗与移动可访问性 | 已完成 | 详细台账保留行动和确认区直接可见，将活动、复盘、节奏、趋势和提醒延迟到可访问卷宗；移动底栏按真实高度为内容和焦点预留空间 |
| M8.22 阅读清晰度、长内容韧性与行动轨迹 | 已完成 | 提升普通正文和元数据字号，关键课程/任务/资料/计划/来源/错误完整换行；首页以静态“事实 → 判断 → 行动”说明智能体闭环，横竖屏五页无页面级横向溢出 |
| M8.23 可见学习流与行动优先首页 | 已完成 | 简洁首页用真实七日任务时间带与 AI 资料收件台连接主行动和首轮闭环；统计仪表盘退入详细视图，三个固定站内入口、真实空状态、长文本与横竖屏已验证 |
| M8.24 DDL 时间议程与任务行动层 | 已完成 | 简洁任务页按本地时间展示逾期、NOW、今天、明天、未来 7 天、稍后、未定日期和已完成；保留完成/反馈/编辑，详细视图承载统计、导出、筛选、完整表格与删除 |
| M8.25 AI 资料收件台与确认优先层 | 已完成 | 简洁资料页按真实处理/识别状态展示最多 6 项与单一主动作；详细管理保留全量能力，无正文、失败读取、候选取消和资料深链均有明确恢复/聚焦 |
| M8.26 近期复习行动板与计划执行层 | 已完成 | 简洁计划页按真实日期和状态给出下一项与最多 7 条近期日程；完成动作不乐观更新且不提交未保存编辑，跨计划延期集合、计划项/无课程计划深链和详细管理均保持可用 |
| M8.27 DDL 学习资料包与安全来源回路 | 已完成 | 简洁任务卡和详细表格显示当前直连资料；删除与未关联状态无失效入口，打开动作经服务端 resolver 与前端严格白名单后才进入资料页 |
| M8.28 首页 DDL 资料就绪带与共享安全导航 | 已完成 | 简洁首页直接呈现 DDL 已关联资料四态，卡片使用同级任务/资料按钮；资料导航共享严格校验并纳入自动化门，手机端保留 API 状态 |
| M8.29 不可复用来源身份与端到端导航保护 | 已完成 | 服务端与目标页面共同核对来源身份；删除重建不冒用旧来源，旧库历史安全降级，旧建议与导出同步保护 |
| M8.30 多页面编辑保护与草稿恢复 | 已完成 | 当前网站提交身份与版本前置条件，过期操作被拒绝；冲突保留草稿并在活动弹窗恢复，过期/替代资料预览不能确认成任务 |

M8.4 已验证：

1. 任务页同时支持快捷完成与可选实际用时/难度反馈，Agent 完成接口返回任务、执行状态和持久回执；
2. 新 DDL、完成、逾期和实际用时变化均留下可追溯事件，并评估现有复习计划；
3. 差异只作用于未完成且未人工修改的基线计划项，旧快照候选不会继续显示；
4. 接受差异前重验计划和任务快照，幂等执行并保存前后快照；
5. 每课程至少 3 个显式样本才形成校准系数，支持解释、边界裁剪和重置，且有效系数用于后续计划生成；
6. 后端 103 个测试、前端 lint/build 及真实浏览器“完成反馈—形成校准—确认重排—重置校准”链路通过。

M8.5 已验证：

1. 服务端按北京时间自然日生成近 7 天半开区间快照，所有指标携带口径与来源；
2. 明确区分“0 项”“暂无证据”和“样本不完整”，估时偏差保留正负方向与缺失样本数；
3. 下周行动最多 3 条，只复用已有候选或固定导航；站内提醒按高中低分级并可持久忽略；
4. 后端 110 个测试、前端 lint/build 通过，真实页面验证了零值显示、偏差展示及“忽略—刷新后仍隐藏”闭环。

M8.6 已验证：

1. 当前复盘会落为一窗一条的本地快照，最多保留 12 条；没有经过真实计算的周次不会补造；
2. 当前窗口可以更新，跨过窗口结束时间后快照冻结，后续读取不会覆盖历史指标、来源或证据摘要；
3. 快照保留计算口径和有限来源，同时过滤原文、路径、密钥等敏感字段，并设定字符串、来源、容量任务和总大小上限；
4. 提醒偏好只控制中低风险的站内展示与即时/每日/每周摘要桶；高风险事实始终可见，显式忽略仍独立且幂等；
5. 后端 116 个测试、前端 lint/build 通过；真实页面验证了历史四项指标、偏好保存持久化和 375px 无横向溢出。

M8.7 已验证：

1. 新增 `POST /api/agent/source-navigation/resolve`：请求只含有界 `source_refs`，服务端仅返回 `/tasks`、`/materials`、`/study-plans` 及其白名单查询参数；未知类型和额外路径字段被拒绝；
2. 任务、资料、计划、计划项、执行回执及固定集合均先在服务端确认可用；已删除或无效来源返回 `available: false` 与空目标，历史名称、摘要和快照保持不变；
3. 首页统一调用 resolver，前端再按固定 path/query 白名单消费结果，不能根据来源 ID 或服务端任意字段自行拼接路由；容量行动也带受控的容量/任务来源；
4. 任务、资料和计划页面已安全消费任务/资料/计划/计划项及固定集合深链，命中对象会显示定位提示并高亮；延期计划集合按固定定义筛选；
5. 后端全量 121 个测试通过，前端 lint/build 通过；真实浏览器已点击任务证据并定位到 `task_id`，375px 总览无页面级横向溢出。浏览器自动化接口未能可靠派发 Enter/Space，因此本次仅确认了原生按钮语义和点击流程，键盘实体按键仍列为 M8.8 的人工验收项。

M8.8 已验证：

1. 新增只读 `GET /api/agent/learning-trends`，在连续 28 个北京时间自然日中只统计具有 `completed_at`、`actual_minutes` 与 `difficulty` 的真实完成反馈；缺失字段不以默认值补齐；
2. 返回学习日分布、最长空档、负荷集中度和完成节奏四项信号，以及窗口、时区、样本数、已知/未知数、限制与来源引用；课程校准只作为既有只读上下文，不回写任何数据；
3. 满足规则的提示仅作为 `review_learning_rhythm` 复核候选，固定 `execution_mode=review`、空输入且没有接受/写入端点；
4. Agent Center 在周度复盘和站内提醒之间展示趋势，不复制复习计划的课程校准面板；趋势证据继续经服务端 resolver 跳转任务，接口失败时明确显示不可用；
5. 修复两个随日期漂移的旧测试构造后，后端全量 124 个测试通过；前端 lint/build 通过；真实浏览器验证趋势区域、受控证据跳转和 375px 无横向溢出。

M8.9 已验证：

1. `WeeklyReviewSnapshot` 新增版本化、脱敏、有界的 `rhythm_summary`；只在当前窗口刷新，窗口关闭后不再重新计算或覆盖；
2. `GET /api/agent/learning-rhythm-history` 只读取关闭快照，且仅在最新两条关闭快照均具备相同摘要版本、计算口径和至少 4 条有效样本时返回比较结果；
3. 旧快照、当前窗口、摘要版本或口径不一致、样本不足均明确返回不可比原因；不补造窗口、不用当前趋势回算历史，也不出现写入或执行动作；
4. 总览在复盘历史与当前趋势之间展示冻结窗口比较；没有关闭窗口时明确提示，不渲染推断图表；
5. 后端全量 129 个测试通过，前端 lint/build 通过；真实浏览器验证不可比降级和 375px 无横向溢出。

M8 稳定性与可访问性强化已验证：

1. 证据导航拒绝控制字符来源标识，并为超长实时标签保留受限显示，避免响应校验失败；
2. 趋势与冻结历史遇到无效反馈、异常校准上下文或畸形摘要时明确降级，不重算历史、更不写入或修复业务数据；
3. 总览、任务、资料和计划页补齐加载状态、错误语义、控件/行操作名称、重复提交保护及窄屏表格滚动；
4. 2026-08-24 全量后端 133 个测试通过，前端 lint/build 通过；仍未做真实屏幕阅读器人工验收。

M8.10 已验证：

1. `GET /api/agent/activity?limit=1..50` 按稳定时间顺序只读合并 `AgentEvent`、`AgentSuggestion`、`ActionReceipt` 和 `AgentRun`；不会刷新建议、物化快照或消费提醒摘要；
2. 接口只返回固定的用户可读标题、说明、状态、时间、最小来源引用和关联 ID；不返回 JSON 载荷、输入快照、文件路径、内部异常或原始建议/回执文本；
3. 未知事件、未知建议状态和不支持来源明确降级为不可用，畸形 `limit` 返回 422；总览仅消费该契约并继续通过来源 resolver 安全跳转；
4. 后端全量 138 个测试通过，前端 lint/build 通过；2026-08-24 在 production preview 中实际加载活动时间线，确认加载状态与 `aria-live` 提示、375px 无页面级横向溢出，并从活动证据安全跳转至任务深链。临时仅为本地 preview 放行 4173 CORS，未修改项目配置；真实屏幕阅读器人工验收仍待补。
5. 活动查询与关联回执查询只加载公共投影所需列；JSON 载荷、输入快照、原始建议标题/说明和回执消息均为延迟敏感列，误访问会失败而不会自动补查。
6. 前端对受支持来源类型采用固定中文标签；服务端 resolver 标签优先，未知类型仅显示“来源”，不会泄露原始类型或拼接未受支持路由。

M8.11 已验证：

1. `POST /api/materials/{id}/extraction/confirm` 在创建正式任务、计划差异候选和更新资料确认状态的同一事务中，记录 `material_extraction_confirmed` 聚合事件；内部载荷只含批次标识与确认任务数；
2. `GET /api/agent/activity` 仅把该事件投影为固定中文标题、说明、`recorded` 状态和 `material` 来源引用，不读取或返回批次、候选、路径或其他内部载荷；
3. 重复确认不会新增事件；强制提交失败会一起回滚任务、资料状态和事件，之后可安全重试；
4. 资料页对已有本地识别结果显示“查看并确认智能识别”，明确外部 AI 仅在用户按次选择后调用，确认后才创建正式任务；
5. 2026-08-24 后端全量 140 个测试通过，前端 lint/build 通过；前端构建仅有既有依赖的 Rollup 注释警告。

M8.12 已验证：

1. `field_evidence` 为课程、资料类型和每个标签保存 `{value, source, snippets}`；来源只可能是文件名、资料正文、两者、本地规则推断或无法确认，DDL 候选继续使用原有 `source_quote`；
2. 服务端先验证最终结构化值，再仅用安全文件名和已解析正文重建证据；忽略 Provider 自报证据，摘录最多两条、每条最多 180 字，且移除 URL 与 Windows/Unix 路径；
3. 新结果的标签数超过 30 会安全拒绝；旧抽取结果缺少该字段时仍可读取为无证据；用户确认时保留原识别证据快照；
4. 资料确认页显示课程、资料类型和每个标签的固定中文来源与摘录；用户修订字段后，旧证据不再冒充当前值，并显式提示原始来源保留；
5. 2026-08-24 后端全量 145 个测试通过，前端 lint/build 通过；production preview 在隔离 SQLite 中实际验证证据卡片和 375px 无页面/弹窗横向溢出。

M8.13 已验证：

1. `GET /api/agent/briefing` 与 `POST /api/agent/refresh` 均返回最多 5 条 `decision_queue` 项；候选只来自资料处理/识别状态、当前有效的计划差异或优先级建议、以及当前容量候选，并使用固定的风险—类型—来源排序和对象去重；
2. 每项只返回稳定决策 ID、固定中文标题/说明、优先级、原因码、单个 resolver-compatible 来源和固定计算依据；不返回建议载荷、Provider 内容、文件路径、任意目标路由或内部异常。过期、缺失和未知来源不进入队列；
3. 队列构建器本身为纯读取，不创建建议、事件、任务、计划、提醒、回执或快照；页面仅能调用既有来源 resolver 导航或复核，不能绕过确认/执行门禁；
4. 首页“今日决策台”对后端响应严格按类型、优先级、原因码、来源和计算依据白名单归一化；畸形项隐藏并诚实降级。队列采用暖白纸面、风险分级和“来源 → 决策”竖向引导，未引入自动执行或额外动效；
5. 2026-08-27 后端全量 150 个测试通过（1 条既有 TestClient/httpx 依赖弃用警告）；前端 lint 与 production build 通过，构建仅保留既有 `@vueuse` PURE annotation 警告。隔离 SQLite 真页面在 375px 下展示两条真实决策，页面宽度 375、内容滚动宽度 360；来源按钮经 resolver 跳转至 `task_id=1`，未触发业务写入。

M8.14 已验证：

1. 应用外壳保留“学伴管家”产品名，以学习台账作为视觉与信息组织语言；桌面侧栏和移动底栏均保持总览、资料、任务、计划、设置五个同级入口，不把同级页面伪装成步骤；
2. 全局持续呈现“资料 → 决策 → 行动”闭环，连接状态、跳到主要内容、键盘焦点和减少动效样式继续保留；
3. 资料页按真实列表状态展示“资料入库—智能识别—人工确认成任务”，明确外部 Provider 按次授权和人工确认门禁；加载、筛选请求或接口失败时隐藏数量，不用假 `0` 冒充事实；
4. 2026-08-27 前端 lint 与 production build 通过，构建仅保留既有 `@vueuse` PURE annotation 警告；隔离 SQLite 真页面在 1440×1000 与 375×840 下完成视觉检查，资料列表显示 1 条真实资料、识别阶段显示 1 条待确认结果，页面均无横向溢出，移动导航触控区均不小于 44px；
5. 本阶段没有修改后端契约；后端 150 个测试为 M8.13 已验证基线，本轮未重复运行。真实屏幕阅读器和完整实体键盘人工验收仍列为 M8 待办。

M8.15—M8.16 已验证：

1. 首页用服务端真实数量生成今日决策、学习收件箱、今日行动和风险中心四个索引入口；简洁视图最多展示 3 条普通决策及有限普通提醒，但所有高风险事实始终保留，详细视图继续提供活动、复盘、冻结比较、趋势和计划差异；
2. 任务与资料在简洁视图中使用纵向卡片，保留完成、反馈、编辑、删除、详情和人工确认等关键动作；导出、课程/类型/标签筛选和完整表格进入详细视图。任务截止日期缺失时明确显示“未设置截止时间”，不伪造时间；
3. 复习计划按“定义输入—核对依据—编辑并保存”的真实操作顺序组织；详细视图保留校准证据、完整风险、来源列和导出。设置页按课程、容量、提醒、外部 AI 建立规则索引，并独立处理各资源加载失败；简洁视图仍明确外部正文可能发送及按次授权边界；
4. 2026-08-28 前端 lint 与 production build 通过，构建仅保留既有 `@vueuse` PURE annotation 警告；隔离 SQLite 的本地后端健康检查和前端入口均返回成功。真实页面在 1440×1000、375×840 和 840×375 下检查总览、任务、资料、复习计划和设置，无页面级横向溢出；任务/资料卡片操作均为 44px；
5. 真页面验收发现并修复决策卡正常返回时仍显示“队列暂不可用”的模板条件链问题；全局视图切换已验证跨刷新保持。此次只修改前端和开发文档，后端 150 个测试仍引用 M8.13 已验证基线，未把未运行的后端测试写成本轮通过。完整实体键盘和真实屏幕阅读器人工验收仍为 M8 待办。

M8.17 已验证：

1. App 路由 `path` 变化后将焦点交给 `main`；skip link 显式聚焦主内容，同页 hash 锚点保持原有定位行为；固定移动底栏处理 safe-area 并预留底部空间，移动输入字号为 16px；
2. 设置页简洁视图提供四块摘要，进入编辑时自动切换详细视图并聚焦对应区块；支持 reduced motion，高风险提醒和外部 AI 按次授权/正文可能外传边界始终显示；
3. 任务与资料简洁卡片区分主要操作和其他操作，详细视图保留完整操作；资料识别入口仅在处理状态为 `processed` 且存在 `extracted_text` 时显示；警告和灰色文本对比度已提升；
4. 2026-08-28 前端 `npm run lint` 和 `npm run build` 通过，build 仅保留既有 `@vueuse` PURE annotation 警告。真实本地页面在 1440×1000、375×840 和 840×375 下验证路由、hash、skip-link、设置展开、任务/资料操作分组、无横向溢出、44px 触控区、16px 输入、底栏不遮挡和控制台无错误；浏览器大字号快捷键未生效，本轮未将其计为已实测；
5. 后端未在本轮重跑，150 个测试仍是 M8.13 的既有基线，不写成本轮通过。

M8.18 已验证：

1. 首页“第一轮学习闭环”只以 `courses_count`、`materials_count`、`active_task_count + completed_task_count` 和 `completed_task_count` 判断四步；空库显示 0/4，已有课程、资料和未完成任务的副本显示 3/4，完成任务后显示 4/4。简洁视图只突出当前一步，详细视图展开四步真实计数；隐藏和恢复均已实测；
2. `create-course`、`upload`、`create-task` 三个固定 action 已在真实页面打开对应对话框，并只移除 `action`，保留 `view` 与 hash。复习计划无课程时的“先添加课程”能直达课程对话框；来源引用仍只通过服务端 resolver；
3. 任务完成反馈在隔离副本中先保存实际用时 75 分钟，再从已完成任务入口更新为 90 分钟；最终 API 返回任务 `completed`、`actual_minutes=90`，证明独立更新没有复用旧幂等结果。真页面还发现并修复了 Element Plus 空星级值 `0` 误阻断“留空完成”的问题；
4. 后端断开并等待 10 秒请求上限后：首页四项统计显示“—”、六项容量显示“待补充”、待确认数显示“—”，闭环进入带重试的加载失败状态，页面只保留一条连接错误，不把失败伪装成零数据；任务、资料加载失败会清空旧列表，深链无结果可安全清除定位；
5. 2026-08-28 前端 `npm run lint`、`npm run build` 和 `git diff --check -- frontend/src` 通过，build 仅保留既有 `@vueuse` PURE annotation 警告；M8.4 的 7 项与 M8.7 的 6 项后端定向测试通过。正常已有数据闭环的浏览器控制台无错误；空库在 375×840 和 840×375 下无页面级横向溢出，闭环按钮高度为 44px。后端 150 项完整基线没有在本轮重跑。

M8.19 已验证：

1. Dashboard 默认简洁视图以 `TodayFocusStage` 展示唯一主行动：优先保留服务端高/严重风险决策，否则使用首条今日行动；其余高风险决策、行动、提醒和容量状态只作为次级注意项，前端不生成新风险或优先级；
2. 点击真实高风险任务“打开任务”已通过服务端 resolver 安全进入 `/tasks?task_id=1` 并定位对应任务；切换详细台账后聚焦完整 Agent Center，原有风险、复盘、趋势、历史和人工确认入口均保留；
3. `EvidenceSourceList` 默认展示 3 条来源，超出部分可展开/收起；来源按钮统一为 44px，目标仍只接受服务端 resolver 返回的站内白名单 `path/query`，不会显示外链或原始路径；
4. 已有数据桌面简洁页高度约 1233px，375×840 移动页约 1664px，均无页面级横向溢出；840×375 横屏下连接重试和主行动均不小于 44px，底部导航未遮挡内容；
5. 隔离空库显示“现在没有需要优先处理的事项”、真实 0 统计和入门进度 0/4，不出现演示行动，控制台无错误。后端断开并等待请求失败后，旧主行动消失、统计显示“—”，页面明确说明不会用旧数据或猜测替代；恢复服务后真实行动重新出现；
6. 2026-08-29 前端 `npm run lint`、`npm run build` 和差异检查通过，build 仅保留既有 `@vueuse` PURE annotation 警告；M8.7 证据导航 6 项定向测试通过。后端 150 项完整基线没有在本轮重跑，真实屏幕阅读器人工验收仍待补。

M8.20 已验证：

1. `DashboardView` 将周度复盘/历史、学习趋势、冻结窗口比较、活动、计划差异和批量来源预热收敛到 `loadDetailedLedgerData`；默认简洁首页只调用 `GET /api/dashboard` 与 `POST /api/agent/refresh`，服务端日志未出现上述详细端点；
2. 首次进入详细视图后并发读取 6 类详细资源，再经 resolver 预热来源；详细状态从加载进入已加载，切回简洁再进入详细视图没有新增服务端请求。详细视图点击“刷新建议”会重新读取 briefing 与详细资源；
3. 详细服务断开时，状态明确列出周度复盘、复盘历史、学习趋势、冻结比较、活动、计划差异和来源入口失败；已加载的主行动继续可见，活动区不回显旧列表，控制台没有代码错误；
4. Element Plus 全量 `dist/index.css` 已替换为当前组件、浮层依赖、`ElLoading`、`ElMessage` 和 `ElMessageBox` 的样式白名单。全局 CSS 从 369.79 kB / gzip 50.93 kB 降至 193.43 kB / gzip 27.53 kB；任务弹窗、遮罩、44px 输入与主按钮、选择下拉均已在真页面复核；
5. `bundle:check` 对全局 CSS 34 KiB、Dashboard JS 42 KiB、Dashboard CSS 8 KiB、最大单 JS 136 KiB 设置 gzip 上限；本次实测分别为 26.89、35.22、6.95、114.81 KiB。缺失/多重匹配/超预算会非零退出；
6. 2026-08-29 `npm run verify`、差异检查和 M8.7 证据导航 6 项定向测试通过；375×840 与 840×375 均无页面级横向溢出，底栏为 74px，主行动与横屏连接重试控件不小于 44px。后端 150 项完整基线未重跑，真实屏幕阅读器人工验收仍待补。

M8.21 已验证：

1. `LedgerDisclosure` 使用原生按钮、`aria-expanded`、`aria-controls`、有标签的 region 和稳定 ID；默认关闭，并只在展开时通过 `v-if` 创建复盘内容。当前判断、收件箱、今日行动、容量、最近处理记录、计划差异和待确认建议保持在卷宗外；
2. 现有数据的桌面详细页在改动前高约 10,983px、包含 40 个活动节点；默认卷宗关闭后高约 3,630px、活动节点为 0，展开后高约 11,091px 并恢复 40 条活动。折叠按钮保持焦点，辅助技术快照可读到“展开复盘与学习节奏”；浏览器实体 Enter/Space 接口未能触发，本轮不把实体键盘写成通过；
3. 移动底栏由 `ResizeObserver` 将实测高度写入 `--mobile-nav-height`，主内容底部 padding、scroll margin、文档 scroll padding 与 safe-area 共同使用该值，并有 resize 回退和卸载清理；
4. 375×840 下底栏高 74px、五个链接各约 62.7px，末尾可操作内容与底栏仍留约 40.6px；840×375 下底栏同样为 74px。卷宗关闭/展开均无页面级横向溢出，视图按钮本体为 44px；
5. `npm run verify` 与前端差异检查通过；构建仅保留既有 `@vueuse` PURE annotation 警告。M8.21 未修改后端、未重跑后端测试；真实屏幕阅读器、实体键盘和系统级大字号仍待人工验收。

M8.22 已验证：

1. 普通元数据和状态标签不低于 12px，解释正文主要为 13px 并使用 1.5—1.75 行高；10px 只保留给 `TODAY / FOCUS`、步骤编号等技术眉题，不承担课程、任务、来源或错误信息；
2. 全局行标题与任务、资料、课程、计划、建议、安全提示、来源和错误原因取消关键省略号，使用 `white-space: normal` 与 `overflow-wrap: anywhere` 保留完整内容；复习计划的超长课程与计划选择项可增长到多行；
3. 设计参考中的 AI Thread 被收敛为首页主行动内的静态“事实 → 判断 → 行动”轨迹，文案固定为“真实数据—当前一步—由你触发”，不伪造来源、不展示模型推理、不新增写操作或授权；3D、星图、压力天气、全局玻璃态和复杂动效明确不进入当前里程碑；
4. 隔离 SQLite 使用超长课程名、资料名、任务名和复习计划，在 375×840 与 840×375 下检查总览、资料、任务、计划、设置五页：页面 `scrollWidth` 均等于 `clientWidth`，关键文本没有 `nowrap/hidden` 截断；横屏计划表步进按钮隐藏，输入区保持 44px；
5. `npm run verify` 通过，gzip 实测为入口 CSS 26.97 KiB、Dashboard JS 35.96 KiB、Dashboard CSS 7.86 KiB、最大 JS 114.97 KiB，均低于预算；build 仅保留既有 `@vueuse` PURE annotation 警告。M8.22 未修改后端、未重跑后端测试；真实屏幕阅读器、实体键盘和系统级大字号仍待人工验收。

M8.23 已验证：

1. `DeadlineFlow` 仅消费 Dashboard 已返回的 `upcoming_tasks`，按 24 小时、1–3 天、3–7 天分组并最多展示 5 项；已完成、已过期、无效时间和 7 天外任务不进入时间带，不在浏览器端推测新风险；
2. 隔离样例暴露 FastAPI 返回的 6 位微秒 ISO 时间被原前 1–3 位毫秒解析器安全过滤；解析器已改为接受标准 ISO 任意小数秒精度，12 小时、2 天和 5 天任务均落入正确分组；
3. `AiInboxShortcut` 仅展示 briefing 中的可确认/需复核/失败真实计数；未知计数不写成 0，确认前不创建任务。“添加资料”打开真实上传对话框，“查看待确认”保留 `view=current_inbox`，任务点击保留 `task_id`；
4. 375×840、840×375 和 1440×1000 下页面 `scrollWidth` 均等于 `clientWidth`；超长课程/任务完整换行，窄屏操作不小于 44px，桌面两栏同顶对齐，控制台无错误；简洁视图不创建统计卡，详细视图不创建两个简洁学习流模块；
5. `npm run verify` 通过，gzip 实测为入口 CSS 26.97 KiB、Dashboard JS 39.69 KiB、Dashboard CSS 9.06 KiB、最大 JS 115.00 KiB。Dashboard CSS 上限因两个有意识的紧凑模块由 8 调整为 10 KiB，Dashboard JS 仍保持 42 KiB 上限。M8.23 未修改后端、未重跑后端测试；真实屏幕阅读器、实体键盘和系统级大字号仍待人工验收。

M8.24 已验证：

1. 新增纯展示/事件上抛的 `TaskAgenda`，不自行请求网络、不生成风险或路由；简洁任务页只消费现有任务、课程和资料数据，详细视图继续复用原统计、筛选、导出、表格与删除能力；
2. 时间议程按本地日期覆盖已逾期、今天、明天、未来 7 天、稍后、未定日期和已完成；NOW 线位于逾期与未来事项之间，风险同时用文字、顺序和边线表达，已完成项显示实际用时或原预计，不伪装剩余工作量；
3. 完成、补充/更新反馈和编辑入口在简洁议程中可用；一次完成动作真实回写 `completed`，进入详细管理后标题获得焦点，`task_id` 深链继续只展示并聚焦命中行；
4. 隔离 SQLite 共 14 项任务覆盖七组、FastAPI 微秒 ISO、超长课程名、实际完成用时与 12 项上限，页面明确显示“另有 2 项”；375×840、840×375、1440×1000 的根 `scrollWidth` 均等于 `clientWidth`，长文本完整换行，议程操作最小 44px，控制台无错误；
5. `npm run verify` 通过，gzip 实测为入口 CSS 26.97 KiB、Dashboard JS 39.69 KiB、Dashboard CSS 9.06 KiB、Tasks JS 15.16 KiB、Tasks CSS 3.39 KiB、最大 JS 115.00 KiB；新增 Tasks JS/CSS 18/5 KiB 上限。M8.24 未修改后端、未重跑后端测试；150 项完整测试仍为 M8.13 既有基线，真实屏幕阅读器、实体键盘和系统级大字号仍待人工验收。

M8.25 已验证：

1. 新增纯展示/事件上抛的 `MaterialInboxFocus`；简洁资料页按真实 `processing_status` 与 `extraction_status` 稳定排序，最多展示 6 项并诚实提示剩余数量，不推断资料级截止时间或伪造扫描进度/置信度；
2. 每份资料只保留与当前状态对应的主动作和必要详情；搜索、上传与确认边界直接可见，完整筛选、标签、下载、编辑、删除和表格留在详细视图，详细表新增独立识别状态列；
3. 无正文详情可直接进入编辑，单条解析重试有忙碌锁，服务断开时不展示旧资料并提供重试；`material_id` 在简洁卡片与详细表格都会滚动、聚焦和标记，`view=current_inbox` 只保留服务端命名集合允许的当前资料；
4. 确认流程只校验和提交已勾选候选。隔离双候选中取消一条并清空其名称后，页面仍成功创建唯一勾选任务，提示“已确认并创建 1 条任务”；
5. 隔离 SQLite 覆盖处理失败、需复核、待确认、识别失败、未识别、处理中、待处理和已确认，包含超长课程/文件名、6 项上限、搜索空状态、后端断开/恢复与三种视口。375×840、840×375、1440×1000 的根 `scrollWidth` 均等于 `clientWidth`，主要按钮和移动候选输入不小于 44px；最终健康页面控制台无错误；
6. `npm run verify` 通过，gzip 实测为入口 CSS 26.97 KiB、Dashboard JS 39.69 KiB、Dashboard CSS 9.06 KiB、Tasks JS 15.15 KiB、Tasks CSS 3.39 KiB、Materials JS 19.57 KiB、Materials CSS 3.79 KiB、最大 JS 114.99 KiB；新增 Materials JS/CSS 24/5 KiB 上限。M8.25 未修改后端、未重跑后端测试；150 项完整测试仍为 M8.13 既有基线，真实屏幕阅读器、实体键盘和系统级大字号仍待人工验收。

M8.26 已验证：

1. 新增纯展示/事件上抛的 `StudyWeekBoard`。简洁复习计划页只消费现有计划及计划项，按已延期、今天、未来 6 个本地日期和稍后稳定排序下一项，最多展示 7 条记录并诚实提示剩余数量；界面使用“近期”而非“本周”，不把条数伪装成自然日，也不生成 AI 优先级、准备度或风险分数；
2. 完成按钮复用现有 `PATCH /api/study-plans/{id}`，请求成功前不改变完成状态，失败时保留原页面。动作只从最近一次服务端响应的计划项基线生成 items-only 更新，不把详细表格中尚未保存的标题、日期或内容顺带提交；单页内有保存锁，跨标签版本冲突仍需后端版本字段后才能解决；
3. 简洁视图完成后把焦点交给新的下一项按钮，若当前延期集合已空则回到“先做这一项”标题；详细管理切换后聚焦完整计划标题，`plan_id/item_id` 会把目标项纳入有界列表并实际聚焦，详细表对应行继续高亮；
4. `view=weekly_plan_delays` 读取全部未归档计划，只保留最近 7 个中国本地日期内未完成的历史项，选择器和提示分别给出全部计划/当前计划数量；合法的 `course_id=null` 计划可凭 `plan_id` 直接打开，不会因缺少课程被清空；
5. 隔离 SQLite 使用 3 份计划验证两门课程延期集合、10 项截断、远期目标深链、完成成功/后端断开、未保存编辑不误提交及无课程计划。375×840 与 840×375 的根 `scrollWidth` 均等于 `clientWidth`，最多 7 张卡片，行动按钮最小 44px；最终健康页面无控制台警告或错误；
6. `npm run verify` 通过，gzip 实测为入口 CSS 26.97 KiB、Dashboard JS 39.69 KiB、Dashboard CSS 9.06 KiB、Tasks JS 15.15 KiB、Tasks CSS 3.39 KiB、Materials JS 19.57 KiB、Materials CSS 3.79 KiB、StudyPlans JS 13.80 KiB、StudyPlans CSS 4.50 KiB、最大 JS 115.00 KiB；新增 StudyPlans JS/CSS 16/6 KiB 上限。M8.26 未修改后端、未重跑后端测试；150 项完整测试仍为 M8.13 既有基线，真实屏幕阅读器、实体键盘和系统级大字号仍待人工验收。

M8.27 已验证：

1. 任务议程把原先的纯文本“来源”改为直接可见的学习资料上下文：只有当前资料列表中仍存在的正整数 `material_id` 才显示真实文件名和“打开资料”；历史删除显示固化名称且没有入口，未关联显示明确空状态并沿用编辑任务入口；
2. 简洁卡片与详细表格共用 `openMaterialSource`。点击后先向 `POST /api/agent/source-navigation/resolve` 提交单个 `material` 引用；前端只接受身份匹配、`available=true`、固定 `/materials` 路径及唯一匹配 `material_id` 查询的普通对象，数组、额外查询键、外链、任意路径和畸形响应都不会路由；
3. 隔离 SQLite 覆盖一条可用长文件名资料、一条删除后保留历史名称的任务和一条未关联任务。可用入口真实进入 `/materials?material_id=1` 并聚焦资料；服务端停机时提示“无法连接后端服务，请确认服务已启动”并停留任务页；
4. 375×840 竖屏和 840×375 横屏的根 `scrollWidth` 均等于 `clientWidth`，资料按钮和任务行动按钮不小于 44px；详细表保留组件内部横向浏览，1440px 宽屏入口可直接点击；最终健康页面三种来源状态同时可见且无控制台警告或错误；
5. 设置页同时去除了简洁视图中容量/提醒失败文案的重复渲染，并为课程编辑、删除补上逐项可访问名称，不改变任何服务端偏好或删除权限；
6. `npm run verify` 通过，gzip 实测为入口 CSS 26.97 KiB、Dashboard JS 39.69 KiB、Dashboard CSS 9.06 KiB、Tasks JS 16.14 KiB、Tasks CSS 3.70 KiB、Materials JS 19.57 KiB、Materials CSS 3.79 KiB、StudyPlans JS 13.79 KiB、StudyPlans CSS 4.50 KiB、最大 JS 115.00 KiB，均低于现有预算；M8.7 证据导航 6 项聚焦测试通过。M8.27 未修改后端业务，150 项完整测试仍为 M8.13 既有基线，真实屏幕阅读器、实体键盘和系统级大字号仍待人工验收。

M8.28 已验证：

1. `DeadlineFlow` 直接消费既有 Dashboard 任务字段；正编号、`source_available=true` 与非空资料名同时成立才显示“打开资料”。历史删除保留名称，未关联明确提示，矛盾数据不给入口；四态由共享纯函数投影，不推断推荐、相关度或准备度；
2. 截止卡片改为 `article` 与同级按钮，无嵌套控件；时间带根据实际可用宽度自动排为一列或多列，长文件名完整换行。任务按钮保留 `task_id` 定位，资料按钮在点击时请求服务端 resolver，Dashboard 与 Tasks 共用严格目标校验；
3. 新增 `navigation:check` 并纳入 `verify`，覆盖资料四态、异常编号、多条响应、来源类型/编号错配、外链、跨页面目标和额外查询参数。矛盾数据为自动化函数验证，本轮没有伪造浏览器服务端响应；
4. 隔离 SQLite 真实页面同时展示可用、历史删除和未关联；资料进入 `/materials?material_id=1`，任务进入 `/tasks?task_id=1`。页面显示可用后删除资料，再点击时明确提示历史资料不存在并留在首页；服务停止时提示无法连接后端且不跳转；
5. 1440×1000、375×840、840×375 根节点均无横向溢出，新增按钮为 44px，手机竖屏操作纵向排列；手机连接标签在在线和离线重载后均可见，简洁/详细按钮与完整详细台账仍可用。健康新页面无控制台警告或错误；
6. `npm run verify` 通过，gzip 实测为入口 CSS 26.97 KiB、Dashboard JS 40.34 KiB、Dashboard CSS 9.17 KiB、Tasks JS 15.96 KiB、Tasks CSS 3.70 KiB、Materials JS 19.57 KiB、Materials CSS 3.79 KiB、StudyPlans JS 13.80 KiB、StudyPlans CSS 4.50 KiB、最大 JS 115.02 KiB，未提高预算；M8.7 证据导航 6 项聚焦测试通过。M8.28 未修改后端业务，150 项完整基线未重跑；真实屏幕阅读器、实体键盘和系统级大字号仍待人工验收。

M8.28 新发现、尚未修复的边界：隔离 API 和当前模型的内存 SQLite 复现均确认资料整数编号可在删除后复用。现有 resolver 只按编号查找，旧页面、活动或复盘引用可能跳到同号新对象；旧计划的 `source_material_ids` 也可能在导出时取得新资料名称。资料删除会清空存活任务的直接关联，能保护刷新后的任务数据，但不能证明历史引用的对象身份。下一优先项为统一“来源身份 v2”：当前实体使用不可变唯一身份，旧库兼容迁移，无法证明身份的旧数字引用安全降级，并覆盖删除—重建同号对象和计划导出的回归。本轮导航校验完成不等于该风险已关闭。

M8.29 已解决上述 M8.28 发现的来源身份缺口。2026-08-31 验证记录：

1. 为资料、任务、复习计划、执行回执及计划项持久化服务端生成的 `navigation_key`。实体来源须同时匹配编号与 key；计划项删除后重添相同 `id` 会产生新 key，客户端不能指定或冒充已保存的来源身份。
2. 保持 `POST /api/agent/source-navigation/resolve`，成功目标的固定查询中增加 key；首页、任务资料入口及来源缓存使用同一身份。资料/任务页不显示不匹配行，计划页在身份失效时收起内容、完成、编辑和导出，只允许清除定位后重新选择；简洁/详细视图均遵守。
3. 新建议、事件、回执、复盘、容量与趋势来源携带其创建时的身份。旧待执行建议缺少身份时失效；即使同号替代任务内容一致，旧建议也不能执行在新任务上。计划导出只按编号与 key 双重匹配查名称，旧裸编号标“历史来源未验证”，错 key 标“来源已失效”，原有文字快照保留。
4. 旧库迁移为当前实体和当前计划项补 key，不按同号当前对象猜测补齐历史引用；迁移可重复执行，身份添加、回填和唯一索引在显式事务内完成。原有纯编号书签仅保留普通当前对象选择，不视为历史身份已验证；畸形或不匹配的显式 key 不降级为纯编号导航。
5. 隔离 SQLite 的真实页面已复现资料和任务删除后均复用编号 1：旧首页资料按钮留在原页，旧资料/任务深链不显示替代对象。计划项 `day-1` 删除后重添，旧深链在两种视图都只留下失效提示和清除定位；显式清除后才恢复普通计划管理。
6. 新资料 key 可正常定位，新计划项 key 会聚焦对应卡片；显式错误 key 不显示替代资料。1440×1000 首页、375×840 失效计划页、840×375 计划与资料页根节点 `scrollWidth` 均等于 `clientWidth`，最终失效页只有一个状态卡与清除按钮，健康页控制台无警告或错误；这是本轮相关页面检查，不替代五页全面辅助技术验收。
7. 后端完整 159 项通过，包含新增 6 项来源身份及 3 项迁移/防冒用回归；前端 `verify` 通过，未提高预算。gzip 实测入口 CSS 26.97 KiB、Dashboard JS/CSS 40.62/9.16 KiB、Tasks JS/CSS 16.26/3.70 KiB、Materials JS/CSS 19.81/3.79 KiB、StudyPlans JS/CSS 14.49/4.59 KiB、最大 JS 115.02 KiB。测试入口为 `backend/tests/agent/test_m829_source_identity.py`、`backend/tests/agent/test_m829_identity_migration.py` 与 `frontend/tests/contracts/check-material-source-navigation.mjs`。

边界：本轮没有迁移或写入用户实际数据库，也没有验证公网部署；真实屏幕阅读器、实体键盘、系统级大字号以及跨标签编辑版本冲突仍待后续 M8。新增身份保证来源归属，不代表资料内容永不编辑，也不把历史快照改写成当前事实。

### M8.30 多页面编辑保护与草稿恢复

2026-08-31 在前述身份保护上增加编辑版本：Terra 负责服务端与前端主实现，Luna Max 补回归，主智能体整合检查、修复预览身份与弹窗边界并完成真实页面验收。

1. 三种实体持久化 `revision`；当前网站提交打开时的身份与版本，缺少合法基线即停止提交。后端原子核对编号、身份和版本，再由 ORM 版本校验保护事务；空 PATCH 不修改版本和更新时间。
2. 覆盖任务编辑/完成/反馈/删除、资料编辑/重试/识别/确认/删除、计划保存/完成/归档；智能体建议和差异在业务快照重验后锁定目标。过期完成和确认不增加回执、反馈或正式任务。
3. 双任务页面实测先保存生效，后保存出现弹窗内恢复卡；查看最新显示截止、优先级、工作量等字段，但任务名称草稿不变。取消“放弃”继续保留，明确放弃后才重读。
4. 资料预览打开后修改正文，旧确认被拒绝且任务数不增加；重读并重新识别后正常确认创建 1 条任务。从详情进入识别仅有一个活动弹窗。删除原资料并创建同号新资料后，旧入口不应用新预览、不显示新候选；预览打开后直接删除资料显示专属不存在提示，不能再确认。
5. 计划详细页保存旧草稿、归档旧计划与简洁页完成旧计划项均被拒绝，服务端新标题和完成数保持不变；查看最新不替换计划名称草稿，明确重读后完成成功，完成数由 0 变为 1。
6. 375×840 任务与资料冲突弹窗、840×375 计划页根节点无横向溢出；恢复按钮实测为 44px。默认桌面检查了同号替代资料的简明恢复卡。此次不替代屏幕阅读器、实体键盘、系统级大字号或五页全覆盖验收；故意冲突/删除请求的 409/404 属于预期结果。
7. 最终后端完整 `183 passed, 1 warning`；新增文件 [`test_m830_edit_concurrency.py`](../../backend/tests/agent/test_m830_edit_concurrency.py) 共 24 项，涵盖三模型 ORM 双 Session 冲突、删除重建竞态、无头兼容、强校验值格式、空更新不变和旧库迁移幂等。前端新增 [`check-edit-precondition.mjs`](../../frontend/tests/contracts/check-edit-precondition.mjs) 并纳入 `verify`，覆盖安全基线、实体专属 404 和预览身份校验；真实弹窗流程另由浏览器验证。
8. 最终前端 `verify` 通过，预算不变：入口 CSS 26.97 KiB、Dashboard JS/CSS 40.62/9.16 KiB、Tasks JS/CSS 17.20/3.70 KiB、Materials JS/CSS 21.06/3.79 KiB、StudyPlans JS/CSS 15.32/4.59 KiB、最大 JS 115.10 KiB gzip。

边界：草稿仅保留在当前页面内存，不跨刷新持久化，也不是完整历史、合并或回滚。旧 API 调用省略版本时仍兼容，课程/偏好与开发清理不属于本轮逐实体契约；先前请求响应丢失时，版本冲突不证明先前一定未保存。用户实际库和公网部署未操作，外部模型慢请求下的并发吞吐未验收。M8.29 记录中的跨标签缺口由本轮网站编辑保护接续，其他人工验收边界仍保留。

### 2026-09-01 主页与邮箱账号扩展（M8 开发增强）

这不是新增比赛里程碑，而是让现有 M8 产品拥有完整入口和个人使用边界：

1. `/` 改为公开产品主页，以真实的“本周课表式学习台账”展示资料进入、DDL 确认、今日行动与复习调整闭环；`/app` 承接登录后的学习总览，原有业务页保持不变。
2. 新增邮箱注册、登录、会话恢复与退出。密码使用随机盐 `scrypt` 哈希；浏览器会话 token 仅存 HttpOnly Cookie，服务端只保存 token 哈希；写操作要求同会话 CSRF token，认证响应禁止缓存。
3. 除主页、注册登录和健康检查外，前端路由与业务 API 均要求有效会话。第一个注册账号接管已有 `data/app.db` 并成为实例管理员；后续账号使用 `data/users/<workspace>.db`，不能读取或修改实例级外部 AI 配置。
4. 上传文件按账号目录隔离；“重置测试数据”只清理当前账号业务库和其可引用文件，不删除其他账号数据。
5. 完整后端回归为 `187 passed, 1 warning`；前端 `npm run verify` 通过，gzip 预算未提高。隔离浏览器验证覆盖桌面主页、375×840 主页与注册页、登录、移动设置账号卡、新增课程、退出以及 `/app` 回到登录页。

边界：本轮使用隔离账号库和合成数据，没有修改用户实际 `data/auth.db`/`data/app.db`，也没有验证公网部署、真实邮件、邮箱所有权、密码恢复、暴力登录防护、备份恢复或生产运维审计。账号隔离仍基于本地 SQLite 文件，不等同于高并发租户平台。

### 2026-09-01 课表与考试扩展（M8 开发增强）

这不是新的 M8 编号，也不改变既有智能体执行边界：

1. 新增 `/schedule` 学期日程页，课表按第 1—30 周和星期展示，支持起止周、每周/单双周、上下课时间、地点和备注；复用课程名称、教师与颜色。
2. 新增考试台账，记录随堂测验、期中、期末或其他考试的开始/结束时间、考场、座位和备注；默认只列出未结束考试，可展开历史。
3. 后端拒绝同一有效周次内真正重叠的课程，允许单双周错开的同一时段；课表与考试均使用不可复用身份、ORM 版本和 `If-Match` 防止旧页面覆盖。
4. 所有考试时刻写入 UTC、按 `Asia/Shanghai` 展示。浏览器验收曾发现响应模型重复转换导致早 8 小时，修复后 14:00 与 09:00 样本均精确显示，并增加后端 UTC 断言。
5. 桌面使用七日课程票根，375×840 使用按天标签与移动课程行；标题、日期和今日结论已收敛为 14—22px 的界面字号，不再以夸张展示字抢占课程信息层级。第 2 周会隐藏单周课程，六项底栏、考试弹窗和页面均无横向溢出，控制台无错误或警告（Vite 开发连接日志除外）。
6. 当轮后端 `191 passed, 1 warning`，前端 `npm run verify` 通过；学期日程 JS/CSS gzip 为 6.93/2.78 KiB，分别低于新增 8/4 KiB 预算。

边界：本轮使用隔离账号和合成课程/考试，没有修改真实用户库。课表周次由用户选择，尚无学期开学日自动推周；课程与考试还没有接入智能体容量、DDL 自动生成或 iCal 导出。

### 2026-09-01 教务日历只读接入（M8 开发增强）

1. `/schedule` 新增“接入教务”入口，支持读取教务系统导出的标准 `.ics`；用户填写来源名称、学期第一周日期和总周数后，服务端把重复事件换算为第 1—30 周的每周/单双周课程，并识别考试关键词、北京时间和地点。
2. 文件先进入不落库预览，课程与考试逐项勾选后才同步；原始日历正文、教务账号和密码均不写入数据库。没有现有课程时，确认同步会按课程名建立课程事实并生成稳定颜色。
3. `AcademicCalendarSyncLink` 只保存来源散列、外部事件 UID、本地不可复用身份和上次应用内容散列。重复导入会返回新增、更新、无变化或跳过；学生在本地修改后不被远端覆盖，已删除本地目标不被自动恢复，远端缺失也不会触发本地删除。
4. 服务端继续执行真实课表冲突校验；同一预览里的重复外部身份、来源标识不匹配、无有效事件、超过 1 MB、全天/不受支持重复规则都会拒绝或明确降级。
5. 接入面板使用 14—18px 的克制界面字号并按需加载。桌面真实完成“选择文件—预览 2 节课和 1 场考试—确认同步—回到七日课表”，375×840 的配置和预览阶段均无页面级横向溢出，控制台无错误或警告。
6. 最终后端 `195 passed, 1 warning`，前端 `npm run verify` 与新增接入面板体积预算通过；主课表 JS/CSS 为 7.18/2.78 KiB，按需接入面板 JS/CSS 为 3.64/1.45 KiB gzip。

当时边界（已由下一节继续推进）：该阶段“接入教务”仅指兼容标准 iCalendar 导出文件的手动只读同步；尚没有针对某一学校的验证码或账号直连适配。

### 2026-09-02 南京理工大学本机教务接入（M8 开发增强）

1. 参考并交叉核验 [NJUST-NannoSchedule](https://github.com/JirakiRance/NJUST-NannoSchedule)、[li_curriculum_table](https://github.com/fans963/li_curriculum_table) 与 [NJUSTSchedule](https://github.com/simpleClover/NJUSTSchedule) 的公开协议和字段；前两者分别为 MIT、GPL-3.0，本项目没有复制 GPL 实现，独立编写了适配和解析代码。第三个仓库当前仅提供版本配置与图片，没有协议实现。
2. 增加固定目标的南理工适配器：优先使用 `8080` 门户 POST 登录并读取 `9080` 教务数据，在旧门户不可达时兼容 `9080` 直连入口；支持验证码、指定学期课表、课程号/教师/教室/节次/周次和考试场次/考场/座位解析。
3. 验证码 Cookie 会话只存在本机进程内存，绑定当前账号、最长 5 分钟且一次性消费；学号、密码、验证码、Cookie、原始 HTML 均不写数据库、文件或日志，Pydantic 校验错误也移除原始输入，避免密码被响应回显。上游请求禁用环境代理、固定学校地址并限制响应大小。
4. 南理工旧教务仍使用 HTTP，因此页面不会把本机直连表述为加密：必须明确确认网络风险后才登录，并建议只在可信校园网或学校 VPN 中使用。项目不提供凭据记忆、后台保活或静默自动同步。
5. `/schedule` 接入面板新增“南京理工大学 / 日历文件”双入口。南理工流程展示“本机 → 南理工教务”数据路径，预览页确认凭据已清除；课程与考试选择确认后才写入当前账号 SQLite，并沿用幂等更新、本地修改保护、删除不恢复和课表冲突规则。第一周日期用于导入后切换当前教学周。
6. 南理工表单拆为独立按需模块以维持原体积预算。2026-09-02 后端完整 `197 passed, 1 warning`；前端 `npm run verify` 通过，主接入面板 JS/CSS gzip 为 4.54/1.72 KiB，未提高 5/2 KiB 上限。测试只使用合成 HTML 和模拟会话，没有提交或调用真实学号密码。

边界：这是面向本地开发与受控使用的非官方适配器，依赖学校现有页面结构和账号政策；学校端点变更、维护或风控都可能使其暂时不可用。当前没有学校官方授权声明，也没有对真实账号登录、校园网/VPN 可达性或账号封禁风险做自动化验收。

### M8.31 Android 移动伴侣 MVP

2026-09-02 完成第一版原生 Android 配套应用：

1. 新增 `android/` Kotlin + Jetpack Compose 单 Activity 工程，界面不是网页套壳。移动端用“今天的时间轨道”呈现智能体下一步建议、待办指标、当天课程和最近考试，底部固定“今天 / 课表 / DDL”三个高频入口；标题控制在 20—24sp，课程与截止信息保持紧凑可读。
2. 课表页支持第 1—30 周切换、星期筛选、单双周说明、教师/教室和考试倒计时；DDL 页读取真实任务并支持完成，成功后重新请求 Dashboard 与任务列表，不做乐观伪完成。设置页明确列出移动端不保存、仅保存和由服务端维护的数据。
3. 移动端复用现有邮箱 Cookie 会话、CSRF 和不可复用身份/版本 `If-Match`。密码只传入一次登录调用，不进入 `ViewModel` 或本地偏好；只接受 `study_session`、`study_csrf` 两个 Cookie，存于进程内存并在退出或进程结束后清除。`SharedPreferences` 仅保存规范化服务地址和当前教学周。
4. 服务地址策略拒绝账号信息、查询参数、锚点和任意路径。Release Manifest 与代码都拒绝 HTTP；Debug 仅允许回环或私有 IPv4，推荐 `scripts/start-android-dev.ps1` 通过隔离 ADB 端口建立 USB reverse，不需要把 FastAPI 暴露到局域网。
5. `testDebugUnitTest` 覆盖 HTTPS 规范化、API 根路径、Release 明文拒绝、Debug 私网白名单、恶意 URL 和北京时间格式；`lintDebug` 与 `assembleDebug` 通过，Debug APK 位于 `android/app/build/outputs/apk/debug/app-debug.apk`。
6. API 36.1、1080×2400 模拟器使用独立数据库和合成课表/考试验证登录、三张主页面、设置隐私边界及任务完成。完成操作把待办从 2 项降为 1 项并进入“最近完成”，证明原生 Cookie + CSRF + `If-Match` 写链路可用；没有触碰真实用户库或真实南理工账号。

边界：当前没有正式签名、应用商店发布、后台通知、离线数据库、生物识别解锁、平板双栏或真机屏幕阅读器验收。安卓端不直接连接南理工教务；用户仍在网页版完成一次性验证码登录、预览和确认同步，移动端只读取已确认的个人工作区数据。

### M8.32 Android“学习航线”视觉优化

2026-09-02 在不改变移动端权限和数据契约的前提下完成视觉重构：

1. 视觉母题从通用卡片列表收敛为“学习航线”。登录页展示“同步事实—决定下一步—按时完成”，底部“今天 / 课表 / DDL”变为连线站点，今日页继续以同一站点语言表达课程与截止任务，形成与学习管理智能体闭环对应的识别系统。
2. 标题由装饰性英文 kicker 改为“今日安排、临近节点、按截止时间、本学期”等真实信息；主标题控制为 19—23sp，正文 14—16sp，时间、周次和计数使用等宽实用字，避免以大字占用移动屏幕。
3. 课表页取消横向滚动，一屏展示七天及有课状态；课程卡采用起止时间票据，考试卡采用倒计时票据，单双周、教师、教室和座位仍来自服务端事实。DDL 卡使用逾期/高优先/进行中状态与圆形完成动作，完成逻辑和失败边界没有改变。
4. 颜色语义固定为墨蓝结构、工科蓝当前、实验绿完成、朱红考试/逾期；普通容器使用细描边而非堆叠阴影。亮色、深色模式分别使用主题前景和 outline，系统状态栏图标通过 Activity edge-to-edge 配置随模式切换。
5. API 36.1、1080×2400 模拟器复核登录、今日、课表、考试、DDL 和深色模式；`testDebugUnitTest`、`lintDebug`、`assembleDebug`、`assembleRelease` 全部通过，Lint 为 0 问题。使用隔离数据库及合成课程/考试，没有接触真实账号或用户库。

边界：本轮是手机视觉和交互层优化，没有增加后台提醒、离线数据库、平板双栏、正式签名或应用商店发布；真机屏幕阅读器、系统级超大字体和低端设备性能仍待后续验收。

明确不在当前阶段处理：正式案例、用户试用报告、技术报告、视频、彩排、报名信息和最终提交包；这些内容统一进入 M9。

### 2026-09-05"同类产品研究与演示模式、专注计时拓展"验证记录

本轮先精读《智能体创新实践大赛参赛手册》与既有[比赛要求对照](../product/比赛要求对照与产品开发方向.md)，并调研 Shovel、TickTick、Classify、MyStudyLife 等同类产品的结构（详见[同类产品研究与功能拓展](../product/同类产品研究与功能拓展.md)）。对照提交要求"涉及登录的，应提供测试账号或演示模式"补齐两项功能，不改变确认门禁与白名单边界：

1. **一键演示模式**：`POST /api/auth/demo-login`（仅 development/test 环境，生产 404 `DEMO_DISABLED`）找到或创建共享演示账号 `demo@study.local`（非管理员、独立工作区、固定测试密码），向其工作区幂等播种 `ensure_demo_data` 演示数据并建立 Cookie 会话；登录/注册页新增"一键体验演示模式"，一次点击以"演示同学"进入带数据的学习总览。演示账号不能访问实例级配置，真实账号数据不受影响。
2. **专注计时器**：新增 `/focus` 懒加载路由与页面（桌面侧栏与任务页入口），支持 15/25/45 分钟预设、开始/暂停/结束；专注满 15 分钟可"完成并记录"或"仅记录用时"，实际用时与任务已有值合并后经既有完成/更新 API 写入并携带 `If-Match`；不足 15 分钟诚实提示不记录。纯逻辑抽出 `utils/focusTimer.js` 并新增 `focus:check` 契约检查，`FocusView` JS/CSS 纳入构建 gzip 预算。

验证：完整后端 `200 passed, 1 warning`（新增 3 项演示登录测试：账号复用与会话、工作区幂等播种、生产禁用）；前端 `npm run verify` 通过（新增 `focus:check`；Focus JS 2.96 KiB / CSS 1.19 KiB gzip，其余预算未上调）。隔离页面实测：登出后从登录页一键进入 `/app`（演示同学），专注页任务下拉出现演示任务，开始/结束状态机与"不足 15 分钟"降级分支正常。边界：演示模式未接入公网部署（online 保持 APP_ENV=production）；专注计时无历史统计、无打断提醒、不自动完成任务；真实屏幕阅读器验收仍留在 M8。

### 2026-09-05“大学生每日体验迭代：连续学习与自动周次”验证记录

本轮面向大学生每天打开产品的两个真实摩擦点迭代（承接[同类产品研究](../product/同类产品研究与功能拓展.md)的后续候选清单）：

1. **连续学习天数（streak）**：Dashboard 新增 `study_streak_days`，按北京本地日期统计“至少完成一个任务”的连续天数；今天尚未完成时 streak 延续到昨天为止（不断档挫败），两天空窗才清零。简洁/详细视图顶部在 streak ≥ 1 时显示“连续学习 N 天”徽标；前端对旧响应宽松降级（缺字段按 0）。
2. **课表自动周次**：`StudyPreference` 新增可空的 `semester_start_date`（轻量迁移同步到系统库与工作区库），课表页新增“按开学日推算本周”提示条：未设置时内联日期输入（周一），设置后显示“今天是第 N 周”，当前查看周不一致时提供“跳到本周”；周次推算抽出 `utils/semesterWeek.js` 并新增 `semester-week:check` 契约检查（周一/周中开学、跨周、30 周边界、非法输入）。

过程中发现并修复一个真实缺陷：轻量列迁移此前只作用于共享库，已存在的工作区数据库拿不到新列（演示工作区读取偏好 500）。现工作区引擎创建时同样重放偏好迁移，并新增迁移回归测试。

验证：完整后端 `207 passed, 1 warning`（streak 纯函数/看板集成/学期日期回读与清除/非法输入/迁移回归）；前端 `npm run verify` 通过（Academic Calendar JS 7.91 KiB gzip，预算内）。隔离真页面实测：演示工作区完成任务后首页出现“🔥 连续学习 1 天”；开学日设为 2026-08-31 后提示“今天是第 1 周”且服务端已保存；手动切到第 5 周出现“跳到本周”并成功跳回。边界：streak 只统计任务完成、不含资料上传或计划完成；自动周次仅作建议定位，不改变任何周次过滤语义；真实学期分历（如寒假跨年）仍由学生自行更新开学日。
